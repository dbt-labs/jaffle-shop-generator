pre-commit:
  commands:
    ruff-format:
      run: .venv/bin/ruff format
    ruff-lint:
      run: .venv/bin/ruff check --fix
    pyright:
      run: .venv/bin/pyright

commit-msg:
  template-checker:
    runner: bash
